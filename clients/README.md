# JARVIS clients

`typescript/` is the supported client facade for deployment planning,
read-only verification, Sui publish-plan generation, and evidence validation.

The client never loads private keys and exposes no broadcast method.
