# Canonical Sui token contracts

- `sui-mainnet/` is the canonical JARVIS Sui Move package profile.
- `sui-testnet/` mirrors the canonical source for Testnet validation and deployment planning.

The cross-chain lock/release vault remains at `contracts/jarvis_bridge/` because it belongs to the Bridge domain, not token issuance.
